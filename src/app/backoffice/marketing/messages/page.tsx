import { PlaceholderPage } from "@/components/backoffice/ui";

export default function MessagesPage() {
  return (
    <PlaceholderPage
      title="Push Messages"
      description="Send push notifications and in-app messages to users."
    />
  );
}
